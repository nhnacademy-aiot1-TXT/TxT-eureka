server:
  port: 8761
eureka:
  instance:
    hostname: instance
  client:
    service-url:
      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@146.56.98.218:8761/eureka/
